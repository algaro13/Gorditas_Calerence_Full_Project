/**
 * Semilla de desarrollo: crea el restaurante "demo" con su administrador y personal.
 * Se implementa sobre POST /api/onboarding/complete en el change `onboarding-module`.
 * Hasta entonces, este comando solo informa.
 */
console.log('[dev-seed] Pendiente: se habilita cuando exista POST /api/onboarding/complete (change onboarding-module).');
console.log('[dev-seed] Mientras tanto puedes crear una organización y un usuario en http://localhost:8080/ui/console');
process.exit(0);
